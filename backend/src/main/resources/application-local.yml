spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:127.0.0.1}:${DB_PORT:5432}/${DB_NAME:manabihub}
    username: ${DB_USERNAME:manabihub}
    password: ${DB_PASSWORD:manabihub_dev_password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true

  # Placeholder configuration for OAuth2 client to satisfy auto-configuration triggers
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1014389452992-8pq9068v81vuq68s5d1vkapubva7uoc0.apps.googleusercontent.com
            client-secret: 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
            scope: openid, profile, email
        provider:
          google:
            issuer-uri: https://accounts.google.com

  
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:thanhmdx12@gmail.com}
    password: ${MAIL_PASSWORD:uvzitlruqqxqzifx}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        transport:
          protocol: smtp

server:
  port: 8081

